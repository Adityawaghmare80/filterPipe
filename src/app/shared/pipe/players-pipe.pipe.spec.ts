import { PlayersPipePipe } from './players-pipe.pipe';

describe('PlayersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
